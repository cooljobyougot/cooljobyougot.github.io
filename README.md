# /site — static repo
- Open `index.html` locally or deploy folder as static hosting.
- All paths are relative (`./assets/...`, `./meta/...`).
- Replace placeholders in `assets/img/` and `assets/video/` with your real files (keep names).
- AI discovery: `/llms.txt` (short) and `/llms-full.txt` (expanded).